# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Secrets — NEVER commit
.env

# Local database (synthetic demo data, regenerable via seed script)
*.db
*.db-journal
*.db-wal
*.db-shm

# Node / frontend
node_modules/
dist/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Scratch experiments (kept local, not part of the deliverable)
scratch/__pycache__/

# Transfer scratch: git bundles Claude writes into the working folder
# when syncing commits from the cloud session. Never part of the deliverable.
Claude outputs/
