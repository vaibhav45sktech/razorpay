[pytest]
testpaths = backend/tests
python_files = test_*.py
addopts = -v --tb=short
filterwarnings =
    # Our own code must not raise DeprecationWarnings — fail the suite if it does.
    error::DeprecationWarning:backend.*
